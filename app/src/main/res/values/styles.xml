<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="fade_in_out_theme" parent="AppTheme">
        <item name="android:windowAnimationStyle">@style/activity_fade_in_out</item>
    </style>

    <style name="activity_fade_in_out" parent="android:Animation.Activity">
        <item name="android:activityCloseExitAnimation">@anim/logo_anim_in</item>
        <item name="android:activityCloseEnterAnimation">@anim/logo_anim_out</item>
        <item name="android:activityOpenExitAnimation">@anim/logo_anim_in</item>
        <item name="android:activityOpenEnterAnimation">@anim/logo_anim_out</item>
    </style>

    <!-- 背景不透明的dialog-->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="popupWindow_default">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>

</resources>
